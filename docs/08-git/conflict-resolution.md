# Conflict Resolution

## Principle
PROPOSED: ownership-first conflict resolution — the module owner of record (per `09-team/ownership-map.md`, once populated) has final say on conflicts within their module's files; cross-module conflicts require explicit coordination between the relevant owners before merge.

## Process
UNKNOWN — no concrete escalation path defined yet beyond "explicit coordination" required by `git-strategy.md`'s prohibition on modifying another employee's module without it.
