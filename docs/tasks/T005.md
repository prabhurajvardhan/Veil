# Task: T005 — Implement Visual Grounding Adapter

## 1. Identity & Routing
- **TASK ID:** T005
- **TASK NAME:** Implement Visual Grounding Adapter
- **OWNER:** AI003
- **MODULE:** M03
- **CURRENT STATE:** BLOCKED
- **INTEGRATION MILESTONE:** M2

### Blocker & Audit Status (AI003)
- **Status:** BLOCKED on missing model weights asset, runtime dependencies, and VLM execution specification.
- **Audited Components (VALID):**
  - BoundingBox & VisualEvidence data contracts conforming strictly to `INTERFACES.md`.
  - Coordinate normalization, scaling (1000-scale and unit-scale), clamping, and parsing in `CoordinateParser`.
  - Screenshot input fail-closed validation.
  - Fail-closed error hierarchy (`VisualPerceptionError`, `InvalidScreenshotError`, `BackendAllocationError`, `ModelInferenceError`, `CoordinateParsingError`).
  - Offscreen document request/response message handling protocol.
- **Removed Non-Compliant Stubs:**
  - Removed placeholder session that returned synthetic `rawPredictions`.
  - Removed `rawPredictionsOverride` test-bypass parameter.
  - Removed arbitrary, ungrounded confidence degradation factors (`0.8` / `0.85`).
- **Exact Blockers Preventing Full Model Execution:**
  1. ShowUI-2B ONNX model weights asset is not present in the repository (requires multi-gigabyte quantized VLM graph).
  2. `onnxruntime-web` dependency is not declared or installed in `package.json`.
  3. Missing repository specification for the VLM prompt template, tokenizer, and input/output tensor pre/post-processing pipeline.
- Per `AI003.md` (§15, §19, §20 Zero-Guessing Rule), the task is blocked pending model asset provisioning and architecture clarification.

### Platform & Runtime Constraints
- **PLATFORM:** Chrome Extension — Manifest V3 (MV3)
- **PRIMARY RUNTIME:** Chrome Extension Offscreen Document Context (WebGPU / WASM)
- **ARCHITECTURAL CONSTRAINT:** This task must be implemented within the VEIL Chrome Extension architecture. A standalone web application is **NOT** an acceptable replacement for the assigned browser-runtime capability.

## 2. Dependencies
| Dependency ID | Type | Description | Status |
|---|---|---|---|
| T003 | TECHNICAL | Implementation prerequisite | VALIDATED |

## 3. Scope & Boundaries
- **IMPLEMENTATION BOUNDARY:** `/src/m03-visual/` (Must follow MODULES.md)
- **ALLOWED FILES:** `/src/m03-visual/*`
- **FORBIDDEN FILES:** Any file belonging to another module or architectural specification outside `/src/m03-visual/`.

## 4. Mandatory References
*You MUST read these specific sections before writing code:*
1. [Decisions](../../DECISIONS.md)
2. [Architecture](../../docs/architecture/ARCHITECTURE.md)
3. [System Design](../../docs/system-design/SYSTEM-DESIGN.md)
4. [Module Registry](../../MODULES.md)
5. [Interfaces](../../INTERFACES.md)

## 5. Implementation Specifications
- **PURPOSE:** Implement the local visual grounding adapter executing ShowUI-2B on raw screenshots using ONNX Runtime Web / WebGPU inside an extension offscreen document.
- **EXACT TECHNOLOGY:** Follow System Design doc exactly. Do not invent mechanisms or libraries.
- **REQUIRED INPUTS:** Described in `INTERFACES.md` and `docs/system-design/SYSTEM-DESIGN.md`.
- **EXPECTED OUTPUTS:** Described in `INTERFACES.md` and `docs/system-design/SYSTEM-DESIGN.md`.
- **IMPLEMENTATION BEHAVIOR:** Strictly follow architectural rules. No mock implementations.
- **ERROR / FAILURE BEHAVIOR:** Fail closed on uncertainty. Propagate errors clearly according to System Design.

## 6. Validation & Handoff
- **ACCEPTANCE CRITERIA:** Component logic strictly satisfies specification, passes all local tests, and operates within the Chrome Extension runtime.
- **VALIDATION METHOD:** Local unit and integration tests executing within or mocking the extension service worker / offscreen document environment.
- **TEST REQUIREMENTS:** 100% core logic coverage for components in this module.
- **VISIBLE RESULT:** Component passes boundary checks and can be bundled into the Chrome extension package.
- **HANDOFF REQUIREMENTS:**
  1. Verify all unit tests pass locally.
  2. Change CURRENT STATE in this file to `VALIDATED`.
  3. Update `TASKS.md` to reflect `VALIDATED` state.
  4. Ping Integration Owner for milestone `M2` handoff.
