# Performance Testing

## What Will Actually Be Measured
UNKNOWN — no target values or metrics have been defined. Per the anti-hallucination rule, do not invent latency, throughput, or resource figures.

## Proposed Categories (PROPOSED, not committed)
- Local perception latency (ShowUI-2B inference time).
- End-to-end loop latency (task → verified result).
- Resource usage during local inference.

Concrete thresholds: VALIDATION REQUIRED — to be defined once V1 is functionally working and can be measured.
